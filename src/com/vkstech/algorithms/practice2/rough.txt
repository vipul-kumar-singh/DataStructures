<Modal
  visible={isEthnicityModalVisible}
  transparent={true}
  animationType="slide"
  onRequestClose={closeEthnicityModal}
>
  <View style={styles.modalContainer}>
    <View style={styles.modalContent}>
      <Text style={styles.modalText}>Select one:</Text>
      {['Hispanic or Latino', 'Not Hispanic or Latino', 'Unknown', 'Prefer not to answer'].map((option) => (
        <TouchableOpacity key={option} onPress={() => handleEthnicitySelect(option)} style={styles.optionContainer}>
          <Text style={styles.radioText}>{option}</Text>
          <View style={styles.radioCircle}>
            {selectedEthnicity === option && <View style={styles.selectedRb} />}
          </View>
        </TouchableOpacity>
      ))}
      <TouchableOpacity onPress={handleSelect} style={styles.bottomButton}>
        <Text style={styles.bottomButtonText}>Label</Text>
      </TouchableOpacity>
    </View>
  </View>
</Modal>





const styles = StyleSheet.create({
  modalContainer: {
    flex: 1,
    justifyContent: 'flex-end', 
    backgroundColor: 'rgba(0, 0, 0, 0.5)', 
  },
  modalContent: {
    width: '100%',
    padding: 20,
    backgroundColor: 'white',
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    alignItems: 'flex-start',
    elevation: 5,
  },
  modalText: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 20,
  },
  optionContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingVertical: 15,
    borderBottomWidth: 1,
    borderBottomColor: '#ccc',
  },
  radioCircle: {
    height: 20,
    width: 20,
    borderRadius: 10,
    borderWidth: 2,
    borderColor: '#196ECF',
    alignItems: 'center',
    justifyContent: 'center',
  },
  selectedRb: {
    width: 10,
    height: 10,
    borderRadius: 5,
    backgroundColor: '#196ECF',
  },
  radioText: {
    fontSize: 16,
  },
  bottomButton: {
    width: '90%',
    backgroundColor: '#0B1F35',
    paddingVertical: 12,
    borderRadius: 25,
    alignItems: 'center',
    alignSelf: 'center',
    marginVertical: 20,
  },
  bottomButtonText: {
    color: 'white',
    fontSize: 16,
    fontWeight: 'bold',
  }
});
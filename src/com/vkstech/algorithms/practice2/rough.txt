{/* Label button at the bottom */}
<TouchableOpacity onPress={handleSelect} style={styles.bottomButton}>
  <Text style={styles.bottomButtonText}>Label</Text>
</TouchableOpacity>


const styles = StyleSheet.create({
  modalContainer: {
    flex: 1,
    justifyContent: 'flex-end', // Align modal to bottom with space on top
    backgroundColor: 'rgba(0, 0, 0, 0.5)', // Dim background
  },
  modalContent: {
    width: '100%',
    height: '90%', // 90% of the screen height
    backgroundColor: 'white',
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    paddingHorizontal: 20,
    paddingTop: 20,
    position: 'relative', // Needed for absolute positioning
  },
  bottomButton: {
    position: 'absolute',
    bottom: 20, // Stick to bottom with some padding
    left: '5%',
    width: '90%',
    backgroundColor: '#0B1F35',
    paddingVertical: 12,
    borderRadius: 25,
    alignItems: 'center',
  },
  bottomButtonText: {
    color: 'white',
    fontSize: 16,
    fontWeight: 'bold',
  }
});